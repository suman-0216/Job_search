{
  "name": "job-hunter-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Automated scripts to fetch job data for the AI Job Hunter dashboard.",
  "main": "scripts/fetch_jobs.js",
  "scripts": {
    "fetch": "node scripts/fetch_jobs.js",
    "clean": "node scripts/clean_old_data.js",
    "run-all": "npm run fetch && npm run clean"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2",
    "@google/generative-ai": "^0.11.3"
  }
}
