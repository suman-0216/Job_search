import SettingsPanel from '../components/SettingsPanel'

export default function SettingsPage() {
  return (
    <div className="apple-shell settings-page-wrap text-[var(--apple-text)]">
      <SettingsPanel />
    </div>
  )
}

